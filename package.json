{
  "name": "tibsa-platform",
  "version": "1.0.0",
  "description": "Run both frontend and backend concurrently",
  "scripts": {
    "setup": "cd frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "dev": "npx -y concurrently -c \"blue,green\" -n \"backend,frontend\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && uvicorn app.main:app --reload",
    "dev:frontend": "cd frontend && npm run dev"
  }
}
